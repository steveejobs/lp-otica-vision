import { Funnel, ReportNotice, ReportTable, SourceBadge } from "@/components/admin/analytics/analytics-ui";import { analyticsDays,PeriodControls } from "@/components/admin/analytics/period-controls";import { RefreshReportButton } from "@/components/admin/analytics/refresh-button";import styles from "@/components/admin/analytics/analytics.module.css";import { AdminPageHeader } from "@/components/admin/admin-ui";import { getConversionReport } from "@/lib/analytics/google-data";import { getInternalAnalyticsReport } from "@/lib/analytics/internal-reports";
export default async function ConversionsPage({searchParams}:{searchParams:Promise<{dias?:string}>}){const days=analyticsDays((await searchParams).dias);const[g,i]=await Promise.all([getConversionReport(days),getInternalAnalyticsReport(days)]);return <div className={styles.stack}><AdminPageHeader eyebrow="Analytics" title="Conversões" description="O objetivo medido é o clique qualificado no WhatsApp; não é tratado como venda."/><div className={styles.toolbar}><PeriodControls basePath="/admin/analytics/conversoes" days={days}/><RefreshReportButton area="conversions"/></div>{g.error?<ReportNotice source="Google Analytics">{g.error}. Conversão por origem e dispositivo permanece vazia.</ReportNotice>:null}<div className={styles.split}><section className={styles.section}><SourceBadge>Dados internos</SourceBadge><h2>Funil comercial</h2><Funnel rows={i.data.funnel}/></section><section className={styles.section}><SourceBadge>Google Analytics</SourceBadge><h2>WhatsApp por origem e dispositivo</h2><ReportTable label="Conversões por origem"><thead><tr><th>Origem</th><th>Dispositivo</th><th>Eventos</th></tr></thead><tbody>{(g.data??[]).map((row,index)=><tr key={`${row.sourceMedium}-${row.device}-${index}`}><td>{row.sourceMedium}</td><td>{row.device}</td><td>{row.events}</td></tr>)}</tbody></ReportTable></section></div><ReportNotice source="Dados internos">As taxas representam passagem entre eventos anônimos agregados. Um clique no WhatsApp não confirma atendimento nem venda.</ReportNotice></div>}
